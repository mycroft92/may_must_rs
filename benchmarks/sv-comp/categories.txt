# SV-COMP source directories for Smash-plus-ultra
#
# Format: one path per line, relative to the sv-benchmarks root.
# Each path is a directory that contains *.c + *.yml pairs.
# Lines starting with '#' and blank lines are ignored.
#
# Only directories with integer / loop / control-flow programs are listed.
# Directories are grouped by the .set file they come from.
#
# To discover more directories, inspect the .set files in the repo:
#   c/Loops.set, c/ControlFlow.set, c/ECA.set, ...

# --- c/Loops.set ---
# Simple loops with integer counters — best fit for invariant synthesis.
c/loops
# Crafted loop patterns and variants.
c/loop-crafted
# Invariant-focused loop benchmarks.
c/loop-invariants

# --- c/ControlFlow.set ---
# Simplified NT driver models — integer state machines, no heap.
c/ntdrivers-simplified
# Lock/unlock control-flow patterns.
c/locks
# Infeasible path programs — exercises assume() / path pruning.
c/infeasible-control-flow
